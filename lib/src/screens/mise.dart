import 'package:flutter/material.dart';

class MiseScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('映え菓子')),
      body: Container(color: Colors.red),
    );
  }
}
